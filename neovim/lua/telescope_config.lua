require("telescope").setup({
        defaults = {
            mappings = {
                n = {
                    ["<C-b>"] = "move_selection_previous",
                }
            }
        }
    })
